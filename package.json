{
  "name": "multilingual-messaging-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @app/infra && npm run build -w @app/lambdas",
    "test": "echo \"No tests yet\" && exit 1"
  },
  "dependencies": {
    "react-native-safe-area-context": "~5.6.0",
    "react-native-screens": "~4.16.0"
  }
}
